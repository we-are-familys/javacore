<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <artifactId>java8</artifactId>
    <groupId>com.hz.java8</groupId>
    <version>0.0.1-SNAPSHOT</version>
  </parent>

  <artifactId>java8-ch01</artifactId>
  <name>java8-ch01</name>
  <!-- 主要介绍了Java8的主要更新内容: 1、行为参数化。2、Lambda表达式。3、流处理。4、默认方法 -->
  <url>http://www.example.com</url>
  
  <!-- 通过本章了解和学习:
  		1、记住语言生态系统的思想,以及语言面临"要么改变,要么衰亡"的压力.
  		2、Java8中新增的核心内容提供的新概念和功能,方便编写即有效又简洁的程序.
  		3、现有的Java编程实践并不能很好的利用多核处理器.
  		4、函数式一等值.方法作为函数式值来传递,Lambda表达式的写法.
  		5、Java8中的Streams概念.
  		6、可以在接口中使用默认方法,在实现类没有实现方法时提供方法内容.
  		7、其他函数式编程的思想.
   -->

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.source>1.8</maven.compiler.source>
    <maven.compiler.target>1.8</maven.compiler.target>
  </properties>

  <dependencies>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.11</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <pluginManagement><!-- lock down plugins versions to avoid using Maven defaults (may be moved to parent pom) -->
      <plugins>
        <plugin>
          <artifactId>maven-clean-plugin</artifactId>
          <version>3.0.0</version>
        </plugin>
        <!-- see http://maven.apache.org/ref/current/maven-core/default-bindings.html#Plugin_bindings_for_jar_packaging -->
        <plugin>
          <artifactId>maven-resources-plugin</artifactId>
          <version>3.0.2</version>
        </plugin>
        <plugin>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.7.0</version>
        </plugin>
        <plugin>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>2.20.1</version>
        </plugin>
        <plugin>
          <artifactId>maven-jar-plugin</artifactId>
          <version>3.0.2</version>
        </plugin>
        <plugin>
          <artifactId>maven-install-plugin</artifactId>
          <version>2.5.2</version>
        </plugin>
        <plugin>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>2.8.2</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
